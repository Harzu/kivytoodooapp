#:import utils kivy.utils
#:import Toolbar kivymd.toolbar.Toolbar 
<Header>:
  text: '' # Сюда будет передоваться текстс который будет передаваться в title
  Toolbar:
    id: action_bar
    md_bg_color: utils.get_color_from_hex('#fcb46d')
    title: root.text
    pos_hint: {'y': .89}
    right_action_items: [['dots-vertical', lambda x: print('1')]]
    